import { Onboarding } from '@/components/onboarding/Onboarding';

export const metadata = {
  title: 'Plante ton arbre — ChangeYourLife.ai',
};

export default function OnboardingPage() {
  return <Onboarding />;
}
