# Sessions - index chronologique

Log incrémental du travail effectué session par session sur changeyourlife.ai.

| Date | Récap | Commits |
|---|---|---|
| [2026-05-08](2026-05-08.md) | Session marathon : audit initial → fixes (PWA, XSS, OTP, nav) + setup workflow + audit profond → cleanup ~2000 LOC + sécurité (Coach auth, XP server-side, Firestore rules durcies) + landing honnête + **Custom Claims** + **DX (Prettier, EditorConfig, scripts npm)** | 9 commits |
| [2026-05-16](2026-05-16.md) | Reprise projet : réparation toolchain (git cassé reconstruit, Node/Vercel/Firebase CLI) + audit complet → Bloc A sécurité (4 XSS, vuln critique protobufjs) + Bloc B nettoyage (imports/deps/code morts) + Bloc D mineurs + **purge token v0 de l'historique git** (filter-branch) + UI (bug codex/autoeval, restyle 404, responsive login/verify-email, nav unifiée) | 7 commits |
| [2026-06-08](2026-06-08.md) | **Journée massive** (SW v92 → v147). Matin : refonte visuelle espace + arbre **ez-tree** + atome + plaque Pioneer + nébuleuse + perf adaptative. Suite : univers Chêne/Frêne/**Architecture** · **badge pixel-art** (+ devient logo/favicon global) · **ORGANIZER vue Canvas + connecteurs** (workflow IA) · **croissance PAR BRANCHE Maslow** sur /app · **SYL = chatbot Claude activé** (clé `API_ANTHROPIC_CHATBOT`) + **cadre strictement non-directif** · **bouton Urgence** + flux de crise (3114/15/112) · **système solaire à l'échelle** · **emojis Fluent 3D** (Twemoji-first + upgrade) · **squelette ESP** (exosquelette aligné, tronc + branches-catégories) | ~50 commits |
| [2026-07-10](2026-07-10.md) | **Fix login Chrome** (COOP `same-origin-allow-popups` + CSP `www.google.com` + redirection explicite `redirectAfterAuth`) - cassé sur Chrome, OK sur Opera · **dark/light toggle global** dans le bandeau · **module Giveaway violet** (timer iOS + cooldown) sur /app · **fiche profil premium** (bordure métal argentée animée + upload avatar drag&drop animé) · **fix diagnostic Google Agenda** (messages d'erreur précis + robustesse popup) · **reorg /app** (l'arbre remonte) · **menu vertical animé** top-right (Home/Notifs/Profil/Paramètres) · **badge ID interactif** (lanyard) sur /profile · fix CSP (bootstraps inline bloqués) · **espace Notifications réel** (store XP/giveaway + badge non-lus) · **fusion profile-card** (bio + lien dans la carte) · **giveaway backend Firestore** (participations cross-device, rules déployées) · **croissance animée de l'arbre** au gain d'XP · **refonte /app** (Aujourd'hui d'abord) · pastille notifs sur le logo · **FIX i18n** (traduction AUTO de tout le DOM + i18n site-wide) · **conformité SYL** (consentement 1re ouverture + modération serveur) · **giveaway back-office + tirage serveur admin** (+ annonce gagnant). SW v147 → v168 | 19 commits |
| [2026-08-08](2026-08-08.md) | **Refonte : l'ORGANIZER devient le cerveau du site.** Changement de cap produit (construire pour l'owner, pas pour un utilisateur imaginaire). **Bug bloquant corrigé** : jeton Google Agenda écrit en `sessionStorage` / relu en `localStorage`, avec scope lecture seule → « Ajouter à l'Agenda » ne pouvait jamais marcher. Nouveau **`/js/gcal.js`** unifié (scope lecture **+ écriture**) · **`/js/organizer-data.js`** (schéma v2 : fiche → branche Maslow + `gcalId`, priorisation, heuristique de rangement) · **`/js/app-organizer.js`** (ORGANIZER embarqué en tête de `/app/` : capture, 4 colonnes Eisenhower, drag & drop, planification agenda) · **SYL centrale** (`/api/syl-brief.js` : brief du jour + **profil type** lus depuis l'organizer ET l'agenda, non-directif, rate-limit 8/h, repli local) · **refonte `/app/`** (accueil minuscule → organizer → SYL → journée fusionnée → arbre relégué à 260 px) · CTA « Coach IA » retiré (une seule IA) · **l'XP part sur la vraie branche** de la fiche. SW v168 → v169 | à venir |
| [2026-08-16](2026-08-16.md) | **Audit complet des 10 dimensions + refonte visuelle de `/app/`.** Reprise du dépôt (3 commits de retard récupérés). **Refonte `/app/`** sur la palette organique : suppression des 11 règles `order:` (l'ordre du DOM redevient l'ordre visuel), fond Vanta + three r134 retirés au profit d'un dégradé CSS, identités de couleur unifiées (ORGANIZER or, SYL or→vert, agenda mousse, giveaway or), grille de modules compactée. **Audit** (4 critiques, 36 importants, 51 mineurs, vérification adversariale, 6 findings réfutés) : ① `/css/` servi en `immutable` 1 an sans versionnage → **les MAJ n'atteignaient jamais les clients** ② `BRANCH_TO_LEGACY` ne mappe que 4 branches sur 8 → **l'XP de l'ORGANIZER était invisible à l'écran** ③ **suppression de compte cassée** (RGPD). **Correctifs** : 2 XSS réels (`/yourlife/`, traductions LLM en `innerHTML`), OTP non lié à son email, plafonds journaliers LLM, CSP resserrée, back-office admin débloqué, npm critical/high éliminées (13→8, 32→10), accessibilité clavier du hub, posture non-directive rétablie. SW v169 → v171 | à venir |
| [2026-08-19](2026-08-19.md) | **La navigation, enfin.** Une seule question, la plus structurelle : où suis-je, où puis-je aller ? La nav tenait dans trois pastilles flottantes en haut à droite. Nouveau **`/js/sidebar.js`** : colonne fixe à gauche façon Vercel/GitHub, 6 destinations principales avec leur raison d'être en sous-titre + 10 modules en liste compacte, page courante surlignée et `aria-current` · **elle adopte, elle ne duplique pas** (langue et thème déplacés, jamais recréés) · **fin du menu déroulant du profil** (il s'ouvrait ancré en haut à droite alors que l'avatar était descendu en bas à gauche) : les 3 actions passent à plat dans le pied · **navigation réorganisée par parcours, pas par module** (3 groupes) · entrée « Frise chronologique » créée · **fix boucle de reflow** dans l'ORGANIZER (le texte tremblait : `overflow-y:auto` rendait l'axe X défilant, `translateX(2px)` au survol faisait apparaître une barre, qui volait le survol, qui annulait le transform…). *Log reconstitué le 26/08.* | 4 commits |
| [2026-08-20](2026-08-20.md) | **La racine devient ton espace.** `public/index.html` supprimé (tant qu'un fichier répondait à « / », la réécriture Vercel n'était jamais atteinte) : la vitrine devient `bienvenue.html`, le témoin `cyl_in` choisit entre espace et vitrine, `/app/` redirige vers « / » · **la fiche ORGANIZER s'ouvrait en double** (panneau local jamais retiré, 172 lignes) - le bloc détresse 3114/15/112 qu'il portait revient **mieux placé** · **le hub s'ouvre de partout** (pied, colonne, fiche) · **CYL a un visage** (professeur en SVG, sans marqueur de genre, animé pendant la recherche) · **i18n : c'était le QUOTA, pas le moteur** → cache Firestore partagé (une langue = un dictionnaire, amorçage ~270 appels une seule fois), puis découverte que les **modèles** Groq/Gemini n'existaient plus → cascade de modèles · **la frise reçoit sa matière** (3 vues : axe, carte mentale XMind, piliers ; 62 nœuds, des pistes pas un formulaire) · **méditation lit l'humeur du jour** (le défaut de fond du site : aucun des 13 modules ne lit ce que les autres savent) · **fond animé unifié** (14 implémentations → `cyl-bg.js`) et **590 couleurs harmonisées** dans 64 fichiers (teintes pivotées, luminosité conservée, critère = chroma) · **2 régressions corrigées le jour même** (barre et chat disparus sur « / », puis mise en page effondrée par une règle `position:relative`). SW v196 → v200. *Log reconstitué le 26/08.* | 13 commits |
| [2026-08-26](2026-08-26.md) | **Audit + une régression critique retrouvée.** Le commit du fond animé (20/08) avait réécrit `common.js` et **perdu le seul `serviceWorker.register()` du site** : pendant 6 jours, aucun nouveau visiteur n'avait ni PWA ni offline, et les bumps de version ne servaient à rien. Réintroduit dans `common.js` + `home-boot.js`. **Zéro CDN de script** : Chart.js (qui existait en **3 versions** selon la page), SortableJS, three r134, vanta et twemoji vendorisés → ferme d'un coup 4 findings du 16/08 (SRI, three CDN, précache illusoire, supply-chain) ; CSP réduite à `'self'` + Google. **A11y** : contraste `--text-3` 4,26:1 → 4,9:1 (AA), `prefers-reduced-motion` respecté par les deux scènes WebGL. **Doc** : `CLAUDE.md` réécrit (il annonçait 34 pages/5 API, l'IA « Lya », et contenait 16 tirets longs alors que la règle l'interdit), logs 19/08 et 20/08 reconstitués, tirets longs purgés. Incident de push 403 (deux comptes GitHub sur la machine). SW v200 → v201 | 5 commits |
| [2026-09-02](2026-09-02.md) | **La traduction était morte depuis treize jours.** Point général croisé sur cinq sources, dont la production Vercel en direct - qui a révélé **143 erreurs sur `/api/translate`**, 4 utilisateurs touchés, quinze langues sans traduction depuis le 20 août. **Les huit modèles de la cascade étaient morts** (Groq 404 et « décommissionné », Gemini « n'est plus disponible »), et la cascade s'arrêtant sur un 429, le dernier repli n'était jamais essayé. Or le 20/08 avait DÉJÀ corrigé ce bug, en remplaçant un nom figé par une **cascade de noms figés** : une liste écrite à la main pourrit, c'est mécanique. **`api/_models.js` interroge désormais l'annuaire de chaque fournisseur** et choisit dans ce qui existe réellement - premier test en production : `gemini-3.5-flash-lite`, un nom absent de tout le dépôt. Budget de temps global (la fonction tournait **300 s** avant d'abandonner). **`/api/health` + cron quotidien + alerte Resend** : la panne avait duré 13 jours en étant journalisée à chaque appel. **`/coach/` retiré** plutôt que réparé (même modèle mort, aucun lien entrant, trois documents du projet le demandaient), page de confidentialité corrigée en conséquence. **Règles Firestore déployées**, en attente depuis mai. « Score Ma Vie » devient « Jalons posés » (une note globale sur une vie contrevient à la règle non négociable). Fin du mur Google à chaque reconnexion. SW v207 → v210 | 6 commits |

---

**Format** : un fichier `YYYY-MM-DD.md` par session de travail.
À chaque fin de session, le fichier est créé/mis à jour avec ce qui a été fait, les commits, les observations et le TODO restant.

Pour fermer la session courante : tape `/session-end`.
