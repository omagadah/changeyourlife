# Session 2026-06-08 - Refonte visuelle majeure (espace, arbre ez-tree, atome)

Très longue session. SW passé de v92 → **v129**. Tout poussé sur `main` (prod Vercel).

## Accueil (page d'accueil, `arbre3d.js` + `tree-model.js`)
- **Satellites + étoiles filantes** ajoutés au cosmos (procéduraux).
- **Arbre ez-tree** : abandon de l'arbre procédural « pas beau ». Génération ez-tree
  AU RUNTIME dans le navigateur (vraies textures feuilles/écorce). `ez-tree-build.js`
  (`buildEzTree(type,{growth,seed})`, presets Chêne/Frêne, `applyGrowth`). Bug clé :
  `loadFromJson()` et pas `loadPreset()` (qui attend un NOM). three vendored = **r184**.
  ez-tree chargé en **différé** (import dynamique) pour la perf.
- Mode `opts.ezTree` dans tree-model : masque le procédural mais garde les 8 nœuds
  Maslow **cliquables** + plateforme + cosmos.
- **Croissance animée par plan de coupe** (tronc→branches→cime), plus de scale « gif ».
- **Atome** (long aller-retour de réglages) : version finale = **3 ellipses** dont les
  normales sont sur un cône (120°), légèrement irrégulières (imperfection), l'orbite
  « open » plus large. Tracés révélés 1 par 1 puis restent. **Satellites cliquables**
  (panneau comme les branches), 4 explicatifs dont le **100% transparent** (primordial,
  + gros) + **SYL** quasi-immobile en bas. Tracé d'orbite visible.
- **Caméra** : rotation auto 360 tant qu'on ne touche pas, stop au clic, reprise à la
  fermeture d'un panneau / après 6 s. Dézoom de repos ~200.
- **Bouton URGENCE (SOS)** bas-gauche → pointillés animés vers le satellite SYL.
- **Ciel = nébuleuse procédurale** (fini les blocs JPEG « Minecraft »). Étoiles 3000.
- **Faille dimensionnelle = plaque de Pioneer** (NASA/Wikimedia PD, `pioneer-plaque.png`)
  flottante, gris pierre + halo, cliquable → explication.

## Login (`login-bg.js`) & /app (`living-tree.js`)
- Login : grand arbre ez-tree (catégorie choisie) + socle + caméra 360-par-dessus.
- /app : **arbre vivant** qui grandit avec l'XP réel + **badge XP fixe haut-droite** +
  toggle Chêne/Frêne. Remplace l'ancien `tree-widget.js` (plus importé).

## Performance & qualité
- **Config adaptative** : palier low/med/high (CPU/RAM/mobile) + **résolution dynamique**
  FPS-aware (reste fluide = Hz max écran).
- **Polish visuel GLOBAL** (via `common.js`, toutes les pages) : transitions, survols,
  focus accessible, halo des champs, scrollbars premium. + shine sur le CTA d'accueil.

## Modules / autres
- **ORGANIZER** : 2 cadenas (verrou colonnes / verrou fiches), état persisté.
- **Accroche d'accueil** retravaillée (eyebrow vert « Font de toi qui tu es... » sous
  « Tes actions, dans la vraie vie », couleurs inversées). i18n SRC_VERSION 5.
- **Agenda en grand** : page `/agenda/` (vue semaine, données client-side).
- **SYL = chatbot Claude** : `api/chat.js` (Anthropic, auth + rate-limit) + widget
  `syl-chat.js` sur /app. **Bloqué tant que `ANTHROPIC_API_KEY` pas dans Vercel.**
- **Avatar image→particules** (`particle-avatar.js`) sur /profile (compact + grand banner).

## Sécurité
- **Audit complet** du repo public : aucune fuite (config Firebase web = publique).
  `.gitignore` durci.
- **Clé API Firebase restreinte** par referrers (Google Cloud) - voir mémoire
  `firebase-api-key-referrer-restriction` (firebaseapp.com OBLIGATOIRE).

## Reste à faire (ROADMAP) - dans l'ordre voulu par l'owner
1. **Badge pixel-art** (depuis l'avatar) - même logique que les particules.
2. **ORGANIZER façon canvas IA** (réf v0 ai-workflow-canvas).
3. **Avatar particules comme avatar global** (haut-droite partout) - à confirmer.
4. **Changement d'univers / thèmes** (arbre ↔ architecture 3D) en 1 clic.
5. **Boutons premium** poussés. **Croissance par branche Maslow.** **App Check.**
6. Activer **SYL** (clé Anthropic). **Protocole mesures d'urgence** (bouton SOS prêt).

> Templates v0 = React/Next → recodés en natif (vanilla) feature par feature, rien à
> télécharger par l'owner.

---

# ───────── Suite du 2026-06-08 (sessions Claude, même jour) ─────────

Très grosse continuation. SW passé de **v129 → v147**. Tout poussé sur `main` (prod Vercel).
La quasi-totalité du « Reste à faire » ci-dessus a été traitée + beaucoup d'ajustements
visuels demandés par l'owner (itérations sur le squelette ESP notamment).

## 1 · Univers Chêne/Frêne + Architecture (commits `4e76672`, `c9b9ab2`, `cfc7786`, `237a9fd`)
- Choix d'essence (Chêne/Frêne) + 2e univers **Architecture** (tour 3D procédurale `archi-build.js`),
  commutable en 1 clic depuis `/profile` (clés `cyl_treeType`, `cyl_universe`). Appliqué
  accueil + login + `/app`.

## 2 · Badge pixel-art + boutons premium (`b6dc357`, `4e88038`, `3ea7df0`)
- Badge pixel-art (sceau) généré depuis l'avatar (`pixel-badge.js`) : rendu rétro = palette
  quantifiée + saturation + contraste.
- Couche « boutons premium » globale (relief + halo de marque + press) via `common.js`.

## 3 · ORGANIZER vue Canvas + connecteurs (`183ec8e`, `96227cb`)
- Bascule **Board/Canvas** persistée. Canvas = toile infinie pan/zoom, fiches positionnables
  (`cx/cy`), nœuds colorés par colonne, grille pointée, **connecteurs/flèches** entre fiches
  (tirer le point d'une fiche → relier ; clic sur un lien → effacer). Vision « workflow IA ».

## 4 · Croissance PAR BRANCHE Maslow sur /app (`0400785`)
- 8 nœuds Maslow autour de l'arbre vivant, taille/halo ∝ XP réel de chaque branche
  (`tree.branches[key].xp`), **croissance LIVE** via l'événement `cyl:xp-gained`, clic → page
  de la branche.

## 5 · SYL = chatbot Claude ACTIVÉ + cadre éthique (`2e9f1f2`, `9842301`)
- **Clé Anthropic** : variable Vercel nommée `API_ANTHROPIC_CHATBOT` ; `api/chat.js` lit les
  deux noms (`ANTHROPIC_API_KEY` || `API_ANTHROPIC_CHATBOT`). SYL démarre.
- **Cadre STRICTEMENT NON-DIRECTIF** (protection juridique + éthique) : system prompt réécrit
  (interdit de prescrire/décider/pousser une idéologie ; sécurité détresse 3114/15/112 ;
  « pas un professionnel »). Wording corrigé (plus de « t'oriente vers la bonne action »).
  Disclaimer visible dans le widget. Voir mémoire `syl-non-directive`.

## 6 · Bouton « Urgence » + flux de crise (`26698fc`, `85e86d1`, + ajustements)
- Bouton rectangulaire vertical **dans l'angle bas-gauche** (renommé depuis « SOS »), sobre.
  Barre SYL du bas alignée à droite. Au clic (`urgence.js`) : triage bienveillant → détresse
  grave = ressources d'urgence immédiates (3114 / 15 / 112 / SMS 114) ; sinon respiration
  guidée + « Parler à SYL ». Anonyme, sans compte.

## 7 · Badge dans le logo global + favicon (`5e545c8`)
- Quand l'utilisateur définit une photo de profil, son **badge pixel-art remplace le logo**
  en haut à droite **sur tout le site** + devient le favicon (`common.js` + `renderPixelBadgeDataURL`).

## 8 · Système solaire à l'échelle + plaque Pioneer (`3a23ebc`, `85e86d1`)
- Soleil ÉNORME, 7 planètes **proportionnelles entre elles** (Terre ~7, Jupiter 77, Saturne 64…),
  distances croissantes, anneaux visibles (échelle compressée, réglable dans `tree-model.js`).
- **Plaque de Pioneer** déplacée loin dans l'espace + agrandie (visible au dézoom max).
- **Panneaux satellites** : seuil de disparition réglable → constante `SAT_PANEL_HIDE_RADIUS`
  (`arbre3d.js`, défaut **3000** ; zoom min 95, repos ~200, max 7000).
- **SYL** repositionné **sur un rayon** d'orbite, mi-hauteur (n'est plus en bas sur le socle).

## 9 · Emojis premium (`5ecd6f4` puis refonte `308e773`)
- Stratégie finale (`emoji.js`) : **Twemoji rendu d'abord** (couverture 100 %, jamais d'emoji
  système moche), puis **upgrade Microsoft Fluent 3D** par probe (swap si l'image charge →
  jamais de 404). Observer = parse des **nœuds ajoutés seulement** → corrige le « site qui
  saute » sur l'accueil 3D. Voir mémoire `emoji-twemoji`.

## 10 · Squelette ESP (exosquelette rayon-X) — longue itération (`eb1ed83` → `692c626`)
- Objectif : effet « ESP/wallhack » blanc qui ÉPOUSE l'arbre, **uniquement sur le tronc + les
  branches qui portent les 8 catégories** (+ sous-familles), pas tout l'arbre.
- Solution retenue (`addEspSkeletonCorridors` dans `ez-tree-build.js`) : wireframe RÉEL de
  l'ez-tree (parfaitement aligné), filtré = **tronc bas + corridor tronc→point-catégorie +
  halos sur les sous-nœuds**. Accueil + `/app` ; login = tronc seul. Construit après la pousse.
- État : **bonne base validée par l'owner**, à affiner plus tard (voir TODO).

## Reste à faire / prochaine session
- [ ] **Squelette précis par branche** : co-construire avec l'owner une **numérotation
  schématique** des branches de l'ez-tree (vue annotée) → mapper EXACTEMENT branche↔catégorie
  pour n'allumer que celles-là (au lieu de l'approximation par corridors). Curseurs actuels :
  `D`, `hubFrac`, `Rnode`, `Rsub` dans `ez-tree-build.js`.
- [ ] Croissance animée (plan de coupe) aussi sur `/app`.
- [ ] Conformité SYL : CGU + mentions légales + consentement 1re ouverture + modération serveur.
- [ ] Références visuelles données par l'owner en session précédente (à re-partager : non
  conservées entre sessions) — ex. v0 `3d-software-engineer-portfolio`, `flowf`, etc.
- [ ] CSP : 3 scripts inline de `index.html` sont bloqués par la CSP (hash/nonce requis) ;
  `ipwho.is` renvoie 403 (géoloc → repli France OK). Non bloquant mais à nettoyer.

## Observations
- Bug pré-existant repéré : violations CSP `script-src` sur 3 inline scripts de l'accueil
  (lignes 736/777/821) — bloqués mais le rendu fonctionne (modules externes OK).
- L'owner travaille beaucoup en VPN/Opera ; jsdelivr reste accessible (emojis OK).
