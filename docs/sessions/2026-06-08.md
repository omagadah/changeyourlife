# Session 2026-06-08 - Refonte visuelle majeure (espace, arbre ez-tree, atome)

Très longue session. SW passé de v92 → **v129**. Tout poussé sur `main` (prod Vercel).

## Accueil (page d'accueil, `arbre3d.js` + `tree-model.js`)
- **Satellites + étoiles filantes** ajoutés au cosmos (procéduraux).
- **Arbre ez-tree** : abandon de l'arbre procédural « pas beau ». Génération ez-tree
  AU RUNTIME dans le navigateur (vraies textures feuilles/écorce). `ez-tree-build.js`
  (`buildEzTree(type,{growth,seed})`, presets Chêne/Frêne, `applyGrowth`). Bug clé :
  `loadFromJson()` et pas `loadPreset()` (qui attend un NOM). three vendored = **r184**.
  ez-tree chargé en **différé** (import dynamique) pour la perf.
- Mode `opts.ezTree` dans tree-model : masque le procédural mais garde les 8 nœuds
  Maslow **cliquables** + plateforme + cosmos.
- **Croissance animée par plan de coupe** (tronc→branches→cime), plus de scale « gif ».
- **Atome** (long aller-retour de réglages) : version finale = **3 ellipses** dont les
  normales sont sur un cône (120°), légèrement irrégulières (imperfection), l'orbite
  « open » plus large. Tracés révélés 1 par 1 puis restent. **Satellites cliquables**
  (panneau comme les branches), 4 explicatifs dont le **100% transparent** (primordial,
  + gros) + **SYL** quasi-immobile en bas. Tracé d'orbite visible.
- **Caméra** : rotation auto 360 tant qu'on ne touche pas, stop au clic, reprise à la
  fermeture d'un panneau / après 6 s. Dézoom de repos ~200.
- **Bouton URGENCE (SOS)** bas-gauche → pointillés animés vers le satellite SYL.
- **Ciel = nébuleuse procédurale** (fini les blocs JPEG « Minecraft »). Étoiles 3000.
- **Faille dimensionnelle = plaque de Pioneer** (NASA/Wikimedia PD, `pioneer-plaque.png`)
  flottante, gris pierre + halo, cliquable → explication.

## Login (`login-bg.js`) & /app (`living-tree.js`)
- Login : grand arbre ez-tree (catégorie choisie) + socle + caméra 360-par-dessus.
- /app : **arbre vivant** qui grandit avec l'XP réel + **badge XP fixe haut-droite** +
  toggle Chêne/Frêne. Remplace l'ancien `tree-widget.js` (plus importé).

## Performance & qualité
- **Config adaptative** : palier low/med/high (CPU/RAM/mobile) + **résolution dynamique**
  FPS-aware (reste fluide = Hz max écran).
- **Polish visuel GLOBAL** (via `common.js`, toutes les pages) : transitions, survols,
  focus accessible, halo des champs, scrollbars premium. + shine sur le CTA d'accueil.

## Modules / autres
- **ORGANIZER** : 2 cadenas (verrou colonnes / verrou fiches), état persisté.
- **Accroche d'accueil** retravaillée (eyebrow vert « Font de toi qui tu es... » sous
  « Tes actions, dans la vraie vie », couleurs inversées). i18n SRC_VERSION 5.
- **Agenda en grand** : page `/agenda/` (vue semaine, données client-side).
- **SYL = chatbot Claude** : `api/chat.js` (Anthropic, auth + rate-limit) + widget
  `syl-chat.js` sur /app. **Bloqué tant que `ANTHROPIC_API_KEY` pas dans Vercel.**
- **Avatar image→particules** (`particle-avatar.js`) sur /profile (compact + grand banner).

## Sécurité
- **Audit complet** du repo public : aucune fuite (config Firebase web = publique).
  `.gitignore` durci.
- **Clé API Firebase restreinte** par referrers (Google Cloud) - voir mémoire
  `firebase-api-key-referrer-restriction` (firebaseapp.com OBLIGATOIRE).

## Reste à faire (ROADMAP) - dans l'ordre voulu par l'owner
1. **Badge pixel-art** (depuis l'avatar) - même logique que les particules.
2. **ORGANIZER façon canvas IA** (réf v0 ai-workflow-canvas).
3. **Avatar particules comme avatar global** (haut-droite partout) - à confirmer.
4. **Changement d'univers / thèmes** (arbre ↔ architecture 3D) en 1 clic.
5. **Boutons premium** poussés. **Croissance par branche Maslow.** **App Check.**
6. Activer **SYL** (clé Anthropic). **Protocole mesures d'urgence** (bouton SOS prêt).

> Templates v0 = React/Next → recodés en natif (vanilla) feature par feature, rien à
> télécharger par l'owner.
