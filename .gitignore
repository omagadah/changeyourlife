.vercel
.firebase
node_modules
.env.local
.env.*.local
*.pem
.DS_Store

# Audit exports (binaires Word)
*.docx

# Logs Firebase CLI (générés au deploy)
firebase-debug.log
firestore-debug.log
ui-debug.log

# Permissions locales Claude Code (per-machine, pas partagé)
.claude/settings.local.json
